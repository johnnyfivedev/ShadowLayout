<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:id="@+id/scroll_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="@dimen/preview_item_margin">

        <include layout="@layout/round_button_samples" />

        <include layout="@layout/button_samples" />

        <!--todo the same with rounded buttons-->
        <!--todo the same with layouts-->
        <!--   <com.johnnyfivedev.ShadowLayout
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               app:sl_dx="@dimen/default_dx"
               app:sl_dy="@dimen/default_dy"
               app:sl_shadowColor="@color/transparent_black">

               <Button
                   android:layout_width="wrap_content"
                   android:layout_height="wrap_content"
                   android:background="@drawable/green_square_button_background"
                   android:text="@string/square_button_text"
                   android:textColor="@color/white" />
           </com.johnnyfivedev.ShadowLayout>-->
    </LinearLayout>
</ScrollView>